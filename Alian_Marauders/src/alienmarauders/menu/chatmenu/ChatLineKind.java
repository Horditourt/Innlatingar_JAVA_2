package alienmarauders.menu.chatmenu;

/**
 * The kind of line displayed in the chat UI.
 */
public enum ChatLineKind {

    /** A normal chat message line (from user). */
    CHAT,

    /** A system message (join/leave/status). */
    SYSTEM
}
